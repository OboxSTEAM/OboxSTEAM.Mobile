---
description: OboxSTEAM backend OpenAPI spec — sync, MCP, envelope mapping for mobile api work
alwaysApply: false
---

# OboxSTEAM Mobile — Backend API Spec

Load when adding or updating `src/lib/api/*`, `src/lib/validations/*`, or any feature that calls the backend. Parent endpoint checklist: `parent-api.mdc`. FE re-copy: `SYNC.md`.

---

## Sources

| Resource | Location |
|----------|----------|
| OpenAPI JSON (committed) | `specs/oboxsteam.openapi.json` |
| Live spec URL | https://api.oboxsteam.website/swagger/v1/swagger.json |
| Swagger UI | https://api.oboxsteam.website/index.html |
| API base (`EXPO_PUBLIC_API_URL`) | https://api.oboxsteam.website |

**Refresh spec:** `pnpm sync:api-spec` (same Swagger as FE — run after BE deploys new endpoints).

**Agent tooling:** Use **obox-api** MCP (`get-api-catalog`, operation lookup) before implementing new endpoints. Prefer the committed spec; sync if stale.

MCP is configured in `.cursor/mcp.json` → `@reapi/mcp-openapi` with `--dir ./specs`.

---

## Swagger → mobile mapping

Backend routes use `/api/...`. Mobile `apiFetch` prepends `EXPO_PUBLIC_API_URL` — pass paths as `/api/...` (same as Swagger / FE).

### Response envelope

Swagger `*ApiResult` schemas map to the shared envelope in `src/lib/api/schemas.ts` (copied from FE):

```json
{ "isSuccess": true, "value": { "code": "...", "message": "...", "data": { ... } }, "error": null }
```

| Swagger | Mobile Zod |
|---------|------------|
| `FooDtoApiResult` outer shape | `createApiResponseSchema(createApiValueSchema(fooSchema))` |
| `value.data` entity | `fooSchema` in `src/lib/api/<domain>/schemas.ts` |
| Message-only success | `createApiResponseSchema(apiValueMessageOnlySchema)` |
| `error` on failure | `assertApiSuccess` → `ApiResponseError` |

Do **not** treat Swagger response bodies as raw DTOs — always wrap with the envelope helpers.

### Implementation checklist

1. Query **obox-api** MCP or read `specs/oboxsteam.openapi.json` for path, method, request/response schemas.
2. Prefer re-copying updated FE modules per `SYNC.md` when the contract already exists on web.
3. Request/form rules → `src/lib/validations/<domain>.ts` (Vietnamese messages).
4. Response entity + `*ValueSchema` → `src/lib/api/<domain>/schemas.ts`.
5. Endpoint function → `src/lib/api/<domain>/index.ts`; slim re-export from `src/lib/api/index.ts`.

Auth: protected routes use Bearer via SecureStore session interceptors (default on `apiFetch`).
