/**
 * Backward-compatible entry for dock padding + tab bar.
 * Prefer importing from `@/components/role-dock`.
 */
export {
  DOCK_CONTENT_PADDING,
  RoleDock,
  RoleDock as AnimatedDock,
} from "@/components/role-dock";
